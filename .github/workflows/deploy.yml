name: Deploy

on:
  push:
    branches: [main]

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build-and-push:
    name: Build & Push Docker Image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/build-push-action@v6
        with:
          context: .
          file: deploy/Dockerfile.gateway
          push: true
          tags: |
            ghcr.io/${{ github.repository }}/gateway:latest
            ghcr.io/${{ github.repository }}/gateway:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    needs: build-and-push
    environment: production
    steps:
      - uses: actions/checkout@v4

      # -------------------------------------------------------
      # Placeholder: replace with your actual deployment steps.
      # Examples:
      #   - SSH into a server and docker compose pull / up
      #   - kubectl apply to a Kubernetes cluster
      #   - Deploy to a managed platform (Fly.io, Railway, etc.)
      # -------------------------------------------------------
      - name: Deploy (placeholder)
        run: |
          echo "Deploy step not yet configured."
          echo "Image: ghcr.io/${{ github.repository }}/gateway:${{ github.sha }}"
